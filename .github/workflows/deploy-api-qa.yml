# ===========================================================
# SKELETON — Registrado no GitHub para workflow_dispatch
# O workflow real executes da branch feature/segregate-qa-env
# ===========================================================

name: Deploy API (QA) v2 (Hosted + Tailscale)

on:
  workflow_dispatch:
    inputs:
      ref:
        description: 'Tag, branch or commit to deploy to QA'
        required: false
        default: 'main'

permissions:
  contents: read

concurrency:
  group: deploy-api-qa

jobs:
  skeleton:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Skeleton — real workflow runs from feature branch via --ref"
